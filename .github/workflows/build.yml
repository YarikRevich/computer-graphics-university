name: Build

on: 
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: "Install prerequisites"
      run: |
        sudo make prepare path=.

    - name: "Build"
      run: |
        sudo make build path=.
